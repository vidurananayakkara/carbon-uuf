{{layout "org.wso2.carbon.uuf.sample.foundation.ui.main"}}
{{title "Session Theme Sample | " @config.appName}}

{{#fillZone "content"}}
    <h3>Session Scoped Theme Sample.</h3>
    <hr />
    <p>You can set an session-scope theme using your executable.
       In your JS file;</p>
    <pre>
        var session = getSession();
        if (!session) {
            var User = Java.type("org.wso2.carbon.uuf.api.auth.User");
            var user = new User("admin", null);
            createSession(user);
        }
        session.setThemeName("org.wso2.carbon.uuf.sample.theme.green");
    </pre>
    <br />
    <h3>Notes:</h3>
    <ul>
        <!-- TODO: Page refresh should not be a requirement for applying the theme -->
        <li>
            Changing theme through the server-side Js will be affected from the next HTTP request.
            To view changed theme; please refresh this page.
        </li>
        <li>
            Setting <code>session-scope</code> theme will affect only for the current session.
        </li>
    </ul>
{{/fillZone}}
